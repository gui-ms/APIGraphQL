type Product {
    id: ID!
    name: String!
    price: Float!
    quantity: Int!
}

type Mutation {
            createProduct(name: String!, price: Float!, quantity: Int!): Product
        }

type Query {
            products: [Product!]!
            product(id:ID): Product!
}